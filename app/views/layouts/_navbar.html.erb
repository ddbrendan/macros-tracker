<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-3">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">Macro Tracker</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav me-auto">
        <a class="nav-link" href="<%= root_path %>">Home</a>
        <a class="nav-link" href="<%= entries_path %>">Entries</a>
        <a class="nav-link" href="<%= foods_path %>">Food Database</a>
      </div>
      <div class="navbar-nav">
        <% if user_signed_in? %>
          <span class="navbar-text me-3">Hello, <%= current_user.email %></span>
          <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "nav-link btn btn-link" %>
        <% else %>
          <%= link_to "Sign in", new_user_session_path, class: "nav-link btn btn-link" %>
          <%= link_to "Sign up", new_user_registration_path, class: "nav-link btn btn-link" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>